# Production, TLS enforced, narrow ranges, catch-all reject
local   all             all                                     peer
hostssl all             app             10.20.30.0/24            scram-sha-256   clientcert=verify-full
hostssl replication     repl            10.20.30.50/32           scram-sha-256   clientcert=verify-full
host    all             all             0.0.0.0/0                reject
host    all             all             ::/0                     reject
