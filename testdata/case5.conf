# mixture with local all trust, hostssl with ssl off flag later maybe, redundant rules
local all all trust
host all all 0.0.0.0/0 scram-sha-256
host all all 0.0.0.0/0 scram-sha-256
hostssl all all 10.10.0.0/16 scram-sha-256 clientcert=verify-full
hostssl all all 10.10.0.0/16 scram-sha-256
host all all 10.10.0.0/16 scram-sha-256
host all all 10.10.1.0/24 scram-sha-256
host all all ::1/128 trust
