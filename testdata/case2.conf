# replication too wide, ident map missing, peer non-local
host replication all 0.0.0.0/0 scram-sha-256
host all all 192.168.0.0/16 ident map=missingmap
host all all 192.168.1.0/24 peer
hostssl mydb app 10.0.0.5/32 scram-sha-256
local all all trust
